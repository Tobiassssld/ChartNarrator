# Optional GPU training and inference dependencies for the ChartNarrator fine-tuning notebooks.
# These versions follow the cleaned notebooks' recorded Colab setup.
#
# Install the core requirements first:
#     pip install -r requirements.txt
#
# Then install this file in a CUDA-enabled environment:
#     pip install -r requirements-train.txt
#
# PyTorch wheels depend on your CUDA/runtime setup. The cleaned notebooks used:
#     pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121

accelerate==1.0.1
bitsandbytes==0.44.1
datasets==2.21.0
deepspeed==0.15.4
einops
flash-attn==2.7.4.post1
peft==0.12.0
protobuf
qwen-vl-utils
scipy
sentencepiece
tiktoken
torch==2.5.1
torchaudio==2.5.1
torchvision==0.20.1
transformers==4.46.1
trl==0.9.6
PyYAML>=6.0.0
