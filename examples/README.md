# Examples

This directory contains compact JSON examples for the ChartNarrator data pipeline.

The examples are derived from representative generated artifacts and shortened for readability. Long prompt, narrative, and time-series strings may contain `...[truncated for example]`.

The examples are intended to show file structure only. They are not sufficient to reproduce the reported experiments by themselves.
